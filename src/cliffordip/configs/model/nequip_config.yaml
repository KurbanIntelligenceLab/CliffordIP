# NequIP model config — data_wrapper interface

model:
  name: nequip
  interface: data_wrapper
  r_max: 6.0
  num_layers: 4
  l_max: 1
  parity: true
  num_features: 44
  radial_mlp_depth: 2
  radial_mlp_width: 72
  num_bessels: 8
  polynomial_cutoff_p: 6
  max_num_elements: 90
